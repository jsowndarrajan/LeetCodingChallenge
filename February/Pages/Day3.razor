@page "/day3"
@using February.Problems

<h4 class="mb-3">
    <a href="@problem.Url" target="_blank" class="ml-md-auto">@problem.Title</a>
</h4>

<div class="mb-3">
    <label for="input" class="form-label">Input</label>
    <input type="text" @bind="Input" class="form-control" id="input" placeholder="[3,2,0,-4]">
</div>
<div class="mb-3">
    <label for="position" class="form-label">Cycle Position</label>
    <input type="text" @bind="Position" class="form-control" id="position" placeholder="1">
</div>
<div class="mb-3">
    <label class="form-label">Constraints</label>
    <ul>
        <li>The number of the nodes in the list is in the range <code>[0, 10<sup>4</sup>]</code>.</li>
        <li><code>-10<sup>5</sup> &lt;= Node.val &lt;= 10<sup>5</sup></code></li>
        <li><code>Position</code> is <code>-1</code> or a <strong>valid index</strong> in the linked-list.</li>
    </ul>
    <p><strong>Follow up:</strong> Can you solve it using <code>O(1)</code> (i.e. constant) memory?</p>
</div>
<div class="mb-3">
    <label class="form-label">Output: <code>@Output</code></label>
</div>

<div class="mb-3">
    <button class="btn btn-primary mb-3" @onclick="Run">Run</button>
</div>

@code {
    private string Input { get; set; }
    private string Position { get; set; }
    private string Output { get; set; }

    LinkedListCycle problem = new LinkedListCycle();

    private void Run()
    {
        Output = problem.HasCycle(Input, Position);
    }
}